import {StyleSheet} from 'react-native';
import {COLORS, SIZES} from '../../../../config/theme';

export const styleProductsRow = StyleSheet.create({
  container: {
    marginTop: SIZES.medium,
    marginBottom: 120,
    marginLeft: 12,
  },
});
