/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { EcommerceApp } from './src/EcommerceApp';

AppRegistry.registerComponent(appName, () => EcommerceApp);
